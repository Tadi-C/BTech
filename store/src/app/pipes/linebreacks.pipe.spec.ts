import { LinebreacksPipe } from './linebreacks.pipe';

describe('LinebreacksPipe', () => {
  it('create an instance', () => {
    const pipe = new LinebreacksPipe();
    expect(pipe).toBeTruthy();
  });
});
